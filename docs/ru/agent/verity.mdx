---
title: "VERITY"
description: "Автономный ИИ-агент с 9 сервисами, защищёнными L402. Зарабатывает sats, платит sats."
---

# VERITY

VERITY — автономный ИИ-агент, построенный на l402-kit. Она продаёт 9 сервисов через протокол L402, накапливает биткоин-казну и самостоятельно корректирует цены в зависимости от спроса — без участия человека.

**Точка обнаружения:** `GET https://l402kit.com/api/verity`

---

## 9 Сервисов

| Сервис | Эндпоинт | Цена | Требует |
|---|---|---|---|
| Веб-поиск | `/api/verity/search` | 100 sats | — |
| Веб-скрейпинг | `/api/verity/scrape` | 200 sats | — |
| Цена BTC | `/api/verity/btc-price` | 10 sats | — |
| Суммаризация | `/api/verity/summarize` | 50 sats | — |
| Анализ тональности | `/api/verity/sentiment` | 30 sats | — |
| Разведка домена | `/api/verity/domain-intel` | 500 sats | — |
| Интеграция l402-kit | `/api/verity/integration` | 10 000 sats | — |
| Состояние мира | `/api/verity/worldstate` | 80 sats | — |
| Перевод | `/api/verity/translate` | 50 sats | — |

Цены динамические — VERITY корректирует их каждые 30 минут в зависимости от спроса.

---

## Как оплатить

Каждый сервис следует стандарту L402:

```bash
# Шаг 1 — запросить сервис (получить счёт)
curl -i https://l402kit.com/api/verity/btc-price

# HTTP/1.1 402 Payment Required
# WWW-Authenticate: L402 macaroon="...", invoice="lnbc..."

# Шаг 2 — оплатить счёт любым Lightning-кошельком
# Шаг 3 — повторить запрос с подтверждением оплаты
curl -H "Authorization: L402 <macaroon>:<preimage>" \
  https://l402kit.com/api/verity/btc-price
```

---

## Автоматическая оплата с L402Client

```typescript
import { L402Client } from "l402-kit/agent";
import { BlinkWallet } from "l402-kit/wallets";

const client = new L402Client({
  wallet: new BlinkWallet(process.env.BLINK_API_KEY!),
  budget: { maxSats: 500 },
});

// Цена BTC — 10 sats
const price = await client.fetch("https://l402kit.com/api/verity/btc-price");
const data = await price.json();
console.log(data.bitcoin.usd);

// Состояние мира — 80 sats
const world = await client.fetch("https://l402kit.com/api/verity/worldstate");
const state = await world.json();
console.log(state.time.utc, state.location.city, state.weather.temperature_c);
```

---

## Справочник сервисов

### Поиск — 100 sats
Веб-поиск, возвращающий топ-10 органических результатов.

```bash
# GET
curl -H "Authorization: L402 <token>" \
  "https://l402kit.com/api/verity/search?q=bitcoin+lightning"

# POST
curl -X POST -H "Authorization: L402 <token>" \
  -H "Content-Type: application/json" \
  -d '{"q":"bitcoin lightning"}' \
  https://l402kit.com/api/verity/search
```

**Ответ:**
```json
{
  "agent": "VERITY",
  "service": "search",
  "query": "bitcoin lightning",
  "results": [
    { "title": "...", "link": "...", "snippet": "..." }
  ],
  "paid_with": "⚡ Lightning L402"
}
```

---

### Веб-скрейпинг — 200 sats
Возвращает полное содержимое страницы в формате markdown.

```bash
curl -X POST -H "Authorization: L402 <token>" \
  -H "Content-Type: application/json" \
  -d '{"url":"https://example.com"}' \
  https://l402kit.com/api/verity/scrape
```

---

### Цена BTC — 10 sats
Цена Bitcoin в реальном времени в USD, EUR и BRL.

```bash
curl -H "Authorization: L402 <token>" \
  https://l402kit.com/api/verity/btc-price
```

**Ответ:**
```json
{
  "bitcoin": { "usd": 98000, "eur": 90000, "brl": 550000 },
  "timestamp": "2026-05-10T12:00:00.000Z"
}
```

---

### Суммаризация — 50 sats
ИИ-суммаризация текстов до 50 000 символов.

```bash
curl -X POST -H "Authorization: L402 <token>" \
  -H "Content-Type: application/json" \
  -d '{"text":"...long text...", "language":"portuguese"}' \
  https://l402kit.com/api/verity/summarize
```

---

### Анализ тональности — 30 sats
Анализ тональности с оценкой, уровнем достоверности и ключевыми словами.

```bash
curl -X POST -H "Authorization: L402 <token>" \
  -H "Content-Type: application/json" \
  -d '{"text":"Bitcoin is the future of money"}' \
  https://l402kit.com/api/verity/sentiment
```

**Ответ:**
```json
{
  "analysis": {
    "sentiment": "positive",
    "score": 0.92,
    "confidence": 0.88,
    "keywords": ["bitcoin", "future", "money"]
  }
}
```

---

### Разведка домена — 500 sats
WHOIS, DNS-записи и SSL-сертификаты. Без API-ключа — использует публичные RDAP и crt.sh.

```bash
curl -H "Authorization: L402 <token>" \
  "https://l402kit.com/api/verity/domain-intel?domain=example.com"
```

**Ответ:**
```json
{
  "domain": "example.com",
  "whois": { "registrar": "...", "registered": "...", "expires": "..." },
  "dns": { "a_records": ["93.184.216.34"] },
  "certificates": [{ "issued": "...", "expires": "...", "issuer": "..." }]
}
```

---

### Интеграция l402-kit — 10 000 sats
Отправьте URL публичного репозитория GitHub. VERITY анализирует кодовую базу и возвращает готовый код интеграции l402-kit.

```bash
curl -X POST -H "Authorization: L402 <token>" \
  -H "Content-Type: application/json" \
  -d '{"repoUrl":"https://github.com/owner/repo"}' \
  https://l402kit.com/api/verity/integration
```

**Ответ:**
```json
{
  "repo": "owner/repo",
  "integration": "## Detected: Express.js\n\n```typescript\nimport { l402 } from 'l402-kit';\n...",
  "next_steps": ["Apply integration code", "Set env vars", "Deploy and test"]
}
```

---

### Состояние мира — 80 sats
Время UTC + геолокация вызывающего + местная погода в одном запросе. Без затрат на внешние API.

```bash
curl -H "Authorization: L402 <token>" \
  https://l402kit.com/api/verity/worldstate
```

**Ответ:**
```json
{
  "time": {
    "utc": "2026-05-10T14:32:00.000Z",
    "unix": 1778421120,
    "hour": 14, "minute": 32, "weekday": "Sun"
  },
  "location": {
    "city": "São Paulo", "country": "BR", "timezone": "America/Sao_Paulo"
  },
  "weather": {
    "temperature_c": 23.4,
    "feels_like_c": 22.1,
    "humidity_pct": 68,
    "condition": "partly cloudy"
  }
}
```

---

### Перевод — 50 sats
Переводит текст или MDX-документацию на любой из 11 поддерживаемых языков. При указании `format` равным `mdx` блоки кода, MDX-компоненты, URL и технические термины сохраняются без изменений — переводится только основной текст.

**Поддерживаемые локали:** `pt`, `es`, `zh`, `ar`, `hi`, `fr`, `de`, `ru`, `ja`, `it`, `en`

```bash
curl -X POST -H "Authorization: L402 <token>" \
  -H "Content-Type: application/json" \
  -d '{"text": "Pay once, access forever.", "locale": "pt", "format": "plain"}' \
  https://l402kit.com/api/verity/translate
```

**Формат MDX** (сохраняет блоки кода и компоненты):
```bash
curl -X POST -H "Authorization: L402 <token>" \
  -H "Content-Type: application/json" \
  -d '{"text": "---\ntitle: Quickstart\n---\n\nPay with Lightning.", "locale": "ja", "format": "mdx"}' \
  https://l402kit.com/api/verity/translate
```

**Ответ:**
```json
{
  "agent": "VERITY",
  "service": "translate",
  "locale": "pt",
  "language": "Brazilian Portuguese",
  "format": "plain",
  "translated": "Pague uma vez, acesse para sempre.",
  "source_length": 26,
  "paid_with": "⚡ Lightning L402"
}
```

Максимальный размер входных данных: 100 000 символов за один запрос.

---

## Динамическое ценообразование

VERITY корректирует цены каждые 30 минут:

```
Высокий спрос (> порогового числа вызовов/час) → цена × 1.1
Нулевой спрос (2 часа подряд)                  → цена × 0.9
Защита минимума                                → цена никогда не опускается ниже минимума
```

Проверить текущие цены в любой момент:
```bash
curl https://l402kit.com/api/verity | jq '.services[].priceSats'
```

---

## API оператора

Если вы форкнули VERITY для запуска собственного агента, эти эндпоинты позволяют изменять цены и бюджеты в режиме реального времени — без повторного деплоя.

Все административные маршруты требуют заголовка `DASHBOARD_SECRET`:
```
Authorization: Bearer <DASHBOARD_SECRET>
```

### GET /api/verity/admin/config
Возвращает текущую конфигурацию цен и расходы потребителя по всем сервисам.

```bash
curl -H "Authorization: Bearer <secret>" \
  https://l402kit.com/api/verity/admin/config
```

**Ответ:**
```json
{
  "configs": {
    "search":  { "base": 100, "floor": 50, "surgeThreshold": 50, "cogs": 2 },
    "translate": { "base": 50, "floor": 50, "surgeThreshold": 30, "cogs": 1 }
  },
  "prices": { "search": 100, "translate": 50 },
  "consumer": { "spent_today": 340, "budget": 10000 }
}
```

### POST /api/verity/admin/config
Обновляет параметры ценообразования для конкретного сервиса. Изменяются только переданные поля.

```bash
curl -X POST -H "Authorization: Bearer <secret>" \
  -H "Content-Type: application/json" \
  -d '{"service": "search", "base": 150, "floor": 80}' \
  https://l402kit.com/api/verity/admin/config
```

| Поле | Описание |
|---|---|
| `service` | Название сервиса (обязательно) |
| `base` | Базовая цена в sats |
| `floor` | Минимальная цена (динамическое ценообразование не опускается ниже) |
| `surgeThreshold` | Количество вызовов/час, при котором срабатывает надбавка +10% |
| `cogs` | Себестоимость в sats (используется фискальным агентом) |

### POST /api/verity/admin/budget
Устанавливает дневной лимит расходов потребителя.

```bash
curl -X POST -H "Authorization: Bearer <secret>" \
  -H "Content-Type: application/json" \
  -d '{"sats": 5000}' \
  https://l402kit.com/api/verity/admin/budget
```

Минимум: 100 sats. Изменения вступают в силу немедленно.

---

## Архитектура

```
VERITY (agent:shinydapps.verity)
  ├── 9 сервисов, защищённых L402
  ├── Казна: shinydapps@blink.sv
  ├── Планировщик: каждые 30 мин (корректировка цен)
  ├── Фискальный агент: ежедневный отчёт (заработанные sats + эквивалент в BRL)
  └── LAW-N: поведенческие события, генерируемые при каждой транзакции
```

VERITY имеет открытый исходный код — та же архитектура может быть форкнута для создания собственного автономного агента.